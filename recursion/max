def find_max(index,nums):
    if index==len(nums)-1:
        return nums[index]
    max=find_max(index+1,nums)
    return nums[index] if nums[index]>max else max
nums=[5,3,12,81,4]
res=find_max(0,nums)
print(res)
