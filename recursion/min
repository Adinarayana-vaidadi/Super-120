def find_min(index,nums):
    if index==len(nums)-1:
        return nums[index]
    min=find_min(index+1,nums)
    return nums[index] if nums[index]<min else min
nums=[5,3,12,81,4]
res=find_min(0,nums)
print(res)
